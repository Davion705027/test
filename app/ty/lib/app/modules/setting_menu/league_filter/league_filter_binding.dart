import 'package:get/get.dart';

import 'league_filter_controller.dart';

class LeagueFilterBinding extends Bindings {
  @override
  void dependencies() {
    // Get.lazyPut(() => LeagueFilterController());
  }
}
